name: WaveHost.eu Auto Renew

on:
  # 定时运行：每4小时执行一次
  schedule:
    - cron: '0 */4 * * *'  # 每4小时执行一次 (0点、4点、8点、12点、16点、20点 UTC)
  
  # 允许手动触发
  workflow_dispatch:

jobs:
  auto-renew:
    runs-on: ubuntu-22.04
    timeout-minutes: 10

    steps:
    - name: Checkout private repository
      uses: actions/checkout@v4
      with:
        repository: kamanmuez/WaveHost.eu-Auto-Renew  # 格式: username/repo-name
        token: ${{ secrets.PRIVATE_REPO_TOKEN }}
        path: scripts
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'scripts/requirements.txt'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r scripts/requirements.txt
        
    - name: Install Chrome and xvfb
      run: |
        # 移除预装的旧版本 Chrome（如果存在）
        sudo apt-get remove -y google-chrome-stable chromium-browser || true
        sudo apt-get autoremove -y
        
        # 安装依赖和 xvfb
        sudo apt-get update
        sudo apt-get install -y wget gnupg xvfb
        
        # 使用新的密钥管理方式（避免 apt-key 弃用警告）
        wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo gpg --dearmor -o /usr/share/keyrings/google-chrome-keyring.gpg
        echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome-keyring.gpg] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
        
        # 全新安装最新版 Chrome
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        
        # 验证安装的 Chrome 版本
        echo "✅ 已安装 Chrome 版本："
        google-chrome-stable --version
        
        # 确保只有一个 Chrome 可执行文件
        echo "📍 Chrome 二进制文件位置："
        which google-chrome-stable
        ls -lh /usr/bin/google-chrome* || true
        
    - name: Run auto renew script
      env:
        # 从 GitHub Secrets 读取配置
        REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
        SERVER_URL: ${{ secrets.SERVER_URL }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        # GitHub Actions 环境标识（自动设置，用于 Telegram 详细链接）
        # GITHUB_ACTIONS, GITHUB_REPOSITORY, GITHUB_RUN_ID 由 GitHub 自动提供
      working-directory: scripts
      run: |
        # 使用 xvfb 运行（有头模式 + 虚拟显示）
        xvfb-run python main.py
        
    - name: Upload screenshots
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: screenshots-${{ github.run_number }}
        path: "scripts/*.png"
        retention-days: 7
        if-no-files-found: ignore
