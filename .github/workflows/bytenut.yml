name: ByteNut Auto Renew

on:
  # 定时触发 - 每6小时运行一次
  schedule:
    - cron: '0 */6 * * *'      # 每6小时的第0分钟运行

  # 手动触发
  workflow_dispatch:

jobs:
  auto-renew:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout private repository
      uses: actions/checkout@v4
      with:
        repository: ${{ github.repository_owner }}/${{ secrets.PRIVATE_REPO_NAME }}
        token: ${{ secrets.PRIVATE_REPO_TOKEN }}
        path: scripts

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: scripts/Bytenut
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Install Playwright browsers
      run: |
        playwright install chromium
        playwright install-deps
    
    - name: Run auto renew script
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      working-directory: scripts/Bytenut
      run: python main.py
    
    - name: Upload error screenshot if exists
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: error-screenshot
        path: scripts/Bytenut/error_screenshot.png
        if-no-files-found: ignore
